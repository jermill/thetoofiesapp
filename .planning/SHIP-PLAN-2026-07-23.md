# Toofies · Plan to raise the ship score — 2026-07-23

Baseline: SHIP-AUDIT-2026-07-23 scored overall **4.5/10**. This plan lists
every gap, what was built this session, and what remains (with owner).

## Built this session (verified live)

| Gap (audit area) | What shipped | Verified |
|---|---|---|
| No tests (Eng 5→) | 24 Vitest specs: economy engine (balance, streaks, forgiveness, quests, recency) + auth validation | `npm test` green |
| No CI | GitHub Actions: lint + test + build on every PR | workflow committed |
| No crash handling | ErrorBoundary with friendly recovery screen (reload / go home), local data untouched | typecheck + manual |
| No security headers | CSP, X-Frame-Options DENY, nosniff, Referrer-Policy, Permissions-Policy | live headers checked |
| Weak input validation | `lib/validate` (email regex, password 8–128, caps) wired into auth | unit tests + sign-in E2E |
| Delete didn't delete (Privacy 3→) | `/api/delete-account` Netlify Function: verifies caller token, deletes their Supabase user; secret key only in Netlify env | live E2E: created→deleted→sign-in fails; unauth = 401 |
| No data portability | JSON export of all local data on Privacy | manual |
| Stub privacy page | Honest policy copy: what's stored where, rights, never-list | shipped |
| No sync (Durability 4→) | Sync v0: capped log snapshot in auth metadata; pull+merge on boot, debounced push; guests untouched | live: metadata snapshot confirmed after sign-in |

## Remaining gaps — founder actions (I cannot do these)

1. **Rotate the Supabase secret key + Netlify token** (both transited chat).
   Then update `SUPABASE_SECRET_KEY` in Netlify env + Cloud Agents secrets.
2. **Enable captcha** on signup: Supabase Dashboard → Auth → Bot and abuse
   protection (Turnstile is free) - blocks scripted signups.
3. **Custom domain** for the demo + Safe Browsing review (Search Console).
4. **Ratify**: economy numbers (D7–D9), design tokens (D11–D15), snark copy
   (R9), real D33 sync schema (tables + RLS) when moving past metadata sync.
5. **Recruit 5–10 beta users** - R5 (zero validation) is still the top risk.

## Remaining gaps — future build work

- Real buddy pairing/cheers over Supabase (needs tables + RLS + realtime) or
  visible de-scope of Buddies for v1.
- Sentry (needs founder DSN) for production error monitoring.
- Sign in with Apple before any App Store submission.
- Split the 3.3k-line CSS file; axe/contrast accessibility pass.
- Moments feed: real or clearly demo-labeled.

## Score movement (estimate, honest)

| Area | Before | After | Why |
|---|:---:|:---:|---|
| Security | 6 | **7** | headers + validation + secret-only deletion path; captcha/rotation still owed |
| Privacy & legal | 3 | **6** | real deletion + export + honest policy; formal GDPR/CCPA text still owed |
| Core loop | 7 | 7 | unchanged (validation is the gap, not code) |
| Feature truth | 4 | 4 | buddies/moments still mocks |
| Data durability | 4 | **6** | cross-device sync v0 + export; table-backed sync still owed |
| Engineering | 5 | **7** | tests + CI + error boundary |
| Design | 6 | 6 | unchanged |
| Distribution | 3 | 3 | founder-gated (domain, review, stores) |
| ED-safety | 8 | 8 | unchanged |
| **Overall** | **4.5** | **~6** | biggest remaining levers: user validation, feature truth, distribution |
